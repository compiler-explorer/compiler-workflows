[project]
name = "compiler-workflows"
version = "0.0.1"
description = "Compiler Explorer workflows and UI"
requires-python = ">=3.12"
dependencies = [
    "click>=8.0.3",
    "boto3>=1.20.5",
    "PyYAML>=6.0",
]

[dependency-groups]
dev = [
    "pytest>=6.2.5",
    "pre-commit>=2.15.0",
]
